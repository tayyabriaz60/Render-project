# Copy this file to .env and replace placeholder values before running locally

# Security (required)
SECRET_KEY=GENERATE_WITH_python_-c_"import_secrets;print(secrets.token_urlsafe(64))"
GOOGLE_API_KEY=REPLACE_WITH_YOUR_GOOGLE_API_KEY

# Gemini AI Configuration (optional - defaults shown)
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
# GEMINI_TIMEOUT_SECONDS=30

# Database
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost/feedback_db
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Initial admin bootstrap (only used if no users exist)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!#

# Application runtime
LOG_LEVEL=INFO
ENVIRONMENT=development
SQL_ECHO=false
AUTO_OPEN_BROWSER=0
PORT=8000


